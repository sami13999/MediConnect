import requests

try:
    # Login as a doctor/patient to get token seems hard without creds, 
    # but maybe I can just inspect the DB directly since I have access to manage.py shell?
    # actually manage.py shell is better.
    pass
except Exception:
    pass
